suite_namespace: lisa
actor: FunctionalTester
modules:
    enabled:
        - \lisa\Helper\Functional
        - Cli
        - Asserts
        - Filesystem
        - TestHelper:
            fixture:
              cleanup_fixture_after_test: false
            mockserver:
              clear_before_test: true   # По-умолчанию false, то есть не чистим моки перед стартом каждого тестa
        - PhpBrowser:
              url: http://gomer.local # Domen вашего приложения
              auth: ['kutsan.k', '123qweASD!']
        - REST:
              depends: PhpBrowser
              url: http://gomer.local # Domen вашего приложения, должен быть такой же как и выше
        - AMQP:
            host: 'rabbit-mq'
            port: '5672'
            username: 'splitter'
            password: 'splitter'
            vhost: '/'
            cleanup: false
            single_channel: true
            queues: [ '_lisa_attachmentsCopy',
                      '_lisa_elastic_sync',
                      '_lisa_elastic_sync_crash',
                      '_lisa_exportGenerating',
                      '_lisa_makeAttachmentsArchive',
                      '_lisa_sendMailNotifications',
                      'goods-service-lisa' ]
        - Yii2: # Интеграция с yii2
              configFile: 'config/test.php'
              part: init
